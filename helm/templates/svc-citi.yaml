apiVersion: v1
kind: Service
metadata:
  name: fashion-store-svc
  namespace: citi-ns
spec:
  type: NodePort
  ports:
    - name: fashion-store
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 32404
  selector:
    app: fashion-store
