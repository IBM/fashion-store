apiVersion: v1
kind: Service
metadata:
  name: fashion-store-svc
  namespace: ci-dev-ns
spec:
  type: NodePort
  ports:
  - name: fashion-store
    protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 32424
  selector:
    app: fashion-store
